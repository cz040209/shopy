# Development-only defaults. Values in backend/.env override these when that
# file exists, so every container uses one consistent set of credentials.
POSTGRES_USER=shopy
POSTGRES_PASSWORD=shopy_local_password
POSTGRES_DB=shopy
